[tox]
skip_missing_interpreters = true
requires = tox>=4.21
env_list = py

[testenv]
description = run the wtforms-tornado test suite
package = wheel
wheel_build_env = .pkg
deps = coverage[toml]
commands =
    coverage run -m unittest discover -s tests -p "test_*.py"
    coverage xml
